Testing steps:
1. Install the NDK r4;
2. Go in jni dir and:
	ndk-build V=1
3. If you have ant, call: ant clean install;
4. If you install eclipse, import this project to Eclipse to run || debug it.

